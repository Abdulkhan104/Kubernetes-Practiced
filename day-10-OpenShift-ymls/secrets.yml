# Create secret for TMDB API key
oc create secret generic hotstar-secrets \
  --from-literal=tmdb-api-key='47de2b9e8b2462b53975d18185ac40bf'

# Create ECR secret (if using AWS ECR)
oc create secret docker-registry ecr-secret \
  --docker-server=655668017312.dkr.ecr.ap-south-1.amazonaws.com \
  --docker-username=AWS \
  --docker-password=$(aws ecr get-login-password --region ap-south-1) \
  --docker-email=your-email@example.com